"""拓展（选学）：用 id() 看对象的内存地址，对应笔记 2.1.4。

id() 返回对象在内存里的地址（十进制整数）。
"""

name = "小明"
age = 18

# id() 看到的就是对象的内存地址（每次运行可能不同）
print(id(name))
print(id(age))

# 重新赋值后，age 指向了新对象，地址会变
age = 20
print(id(age))     # 和上面 age=18 时的地址不同
