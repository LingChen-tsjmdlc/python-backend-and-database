"""交互模式演示，对应笔记 1.10。

交互模式在终端输入 python 进入，逐行输入、立即出结果，本身不保存为文件。
下面用注释还原交互模式的样子，并给出等价的脚本写法。

    终端里进入交互模式后：
    >>> print("hello")
    hello
    >>> exit()      退出交互模式

把上面的输出，用脚本写出来等价如下：
"""

print("hello")
