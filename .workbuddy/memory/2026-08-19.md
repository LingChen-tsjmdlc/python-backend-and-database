# 2026-08-19
- homework/ P0 环境完成：uv venv + herosideui==0.8.0(TestPyPI explicit 索引，sources 单包指向) + PySide6 6.11.2 + pydantic + pyyaml + pytest/pytest-qt。发现：herosideui 0.8.0 将 pyside6>=6.11.0 声明为硬依赖（无 pyside6 extra，只有 dev），[pyside6] 写法无效已移除。pyproject 改为虚拟项目（删 build-system/scripts/src，应用型项目不需要构建自身）。uv 装在 C:/Users/Administrator/.local/bin，PATH 已永久配置但终端宿主需重启才继承新 PATH。
- homework/ P1 core 层完成并验收：core/{schema,loader,parse,grader}.py + tests/（conftest fixture 卷 5 题覆盖 4 题型）+ pytest.ini（pythonpath=. 解决虚拟项目导入；tests 加 __init__.py 支持相对导入）。44 测试全绿。要点：pydantic 判别联合 frozen 模型；分值≠100 只 warning；parse 只 ast.parse 不执行（256KB 上限）；grade() 返回 auto/manual 两段报告，apply_manual_score() 函数式写回（pending→graded_manual，未作答不给分、超分拒绝）。
- homework/ P2 第一份题库完成：part1_python/section2.yaml，17 题（6单选+4填空+3代码分析+2代码解析+2编程），总分 100 无警告（分值 4×6+4×4+6×3+6×2+15×2）。题目口径对齐 notes/part1_python/section2.md。yaml 陷阱实录：裸 3/3.5/False 会被 YAML 推断成 int/float/bool（须加引号），points 必须 int（7.5 被拒，改整分明案）。全对预演 100/100，部分对预演多空均分正确，44 测试回归全绿。
- homework/ P3+P4 完成：app/（main.py 入口 + window.py 主窗口：Listbox 章节/题目滚动区/进度条/提交栏 + renderers/ 四题型渲染器 + registry）。API 核对自组件库 docs/：Title 用 level=（非 size=），RadioGroup 用 create_radio()（add_radio 是底层方法）。已知问题：pytest-qt 在本沙箱跑 HeroSideUIProvider.setup() 会因 sitecustomize 文件垫片触发 tempfile Bad file descriptor（GUI 测试用户本机跑）；同因 python -c 里 unlink 临时文件会 PermissionError，均为环境副作用非代码问题。用户明确：GUI 不用我测，他自己跑。
- homework UI 首跑三连修（均为组件 API 与 Qt 惯用名差异，逐条对照 .venv 内 0.8.0 源码核实）：① Listbox.selected_keys() 返回 set，不能下标，用 next(iter())；② Chip 是自绘 QWidget 无 setText，用 set_text()（Textarea 同：text()/set_text()，无 toPlainText/setPlainText）；③ Card 自带三层布局（CardHeader/CardBody/CardFooter + add_body()），不能 QVBoxLayout(card) 强塞（17 条 QLayout 警告）。offscreen 构建验证：18 卡槽（17题+stretch）、零警告。组件库 backlog：API 命名不一致（set_text vs setText）值得统一或文档标注。uv run homework 入口已配好（hatchling packages=[app,core]）。
- UI v2 改版（用户设计）：左 Accordion×Button（六部分×每节作业按钮，content_widget 放按钮容器，有作业的部分默认展开）+ 垂直 Divider 分割 + 右 Title/Chip 头部 + 题型 Tabs（underlined，四 Tab=单选·代码分析/填空/代码解析/编程，每 Tab 一个 QScrollArea 内放 Card 题卡）+ 底部 Progress + full_width 提交按钮。ThemeSwitcher 留侧栏底部。Tabs API：add_tab(title, widget, key=)；重建 Tabs 用 deleteLater+新建。herosideui 0.8.1（新增代码编辑组件）pyproject 已钉、venv 待用户 uv sync（本地仓库 git log 尚未见该提交）。测试 test_app_gui.py 已同步 v2 断言。
- UI 五条反馈修正：① 侧栏不撑满高度（stretch=0 + AlignTop，ThemeSwitcher AlignBottom + addStretch）；② Divider 24px 是我 setFixedWidth(24) 造成的（组件原生 1px），删掉该行，间距用布局 spacing=16 给；③ 全部组件回归默认样式（Tabs/Accordion/Button 不再传 variant/color/size，只留 full_width 等结构性参数）；④ 题目去 Card 黑框（直接 Subtitle+Text+renderer 入滚动区）；⑤ 删 Progress 进度条和总分 Chip（连带 _update_progress 整个方法）。教训入库：不要自作主张改组件默认样式变体。offscreen 验证 17 渲染器干净构建。
- QPainter 崩溃修复：根因是我 _render_tabs 里 deleteLater 整个 Tabs 再新建——cursor 动画组件（_CursorWidget）悬挂引用导致 "paint device painted by one painter" + "already deleted"。组件官方姿势是 Tabs.clear() 后复用实例（docs/tabs.md 方法表）。offscreen 验证连续两次 clear+重建 4 tab/17 渲染器零报错。流程教训：写 UI 代码前必须先读 docs/ 对应组件的完整文档（含方法表），不能只看构造签名。
- QPainter 报错复现排查：用户报 uv run homework 仍 QPainter::begin 报错，但同机真实平台（非 offscreen）完整跑：初始渲染/重选作业/4 Tab 轮切/手风琴折叠展开/主题来回切/暗色下重选——全部零报错（editable 安装确认走源码、uv run homework 入口 15s 驻留零输出）。判断：报错来自用户端旧进程/旧窗口残留或另一环境；当前 E:/.../homework 源码验证干净。herosideui 已是 0.8.1。
- 左侧改平铺（用户决定放弃 Accordion）：Subtitle 部分标题 + flat 按钮列表，外裹 NoFrame QScrollArea，ThemeSwitcher 仍在底部。Accordion/AccordionItem 导入与 _accordion 全部移除。真平台验证平铺构建+重选+4Tab 轮切零报错，core 44 测试回归全绿。
- 编程题接入 CodeEditor（0.8.1 新组件，按 docs/code_editor.md）：value()/set_value()/text_changed，Tab 空格缩进+冒号自动缩进内置。渲染器 starter 预填；ProgrammingAnswer 加 source 字段（file/source 二选一）；core/parse.py 新增 parse_py_source（AST 只解析不执行）；提交时 source 落盘 submissions/<卷>/<题>/answer.py 归档后按 file 判分。offscreen 验证预填/作答/收集全通；46 测试全绿（含 source 判分+语法错误两新用例）。
- 侧栏 Divider 撑出滚动修复：水平 Divider 的 size policy 是 (Expanding, Preferred)，在 QVBoxLayout 中垂直方向可增长，撑爆 QScrollArea 视口。修法：setSizePolicy(Expanding, Fixed) 锁死 1px 高。验证 5 条分割线全部 height-locked。组件库观察点：Divider 可考虑在 set_orientation 时按方向自设 Fixed 正交轴 policy，省得使用方踩坑。
- 侧栏双轴滚动根治：①横向元凶是部分标题 Text（QLabel 默认不换行，"第 2 部分 · 数据库" 文字宽 246 > 视口 234）→ setWordWrap(True)；②纵向元凶是五个"暂无作业"占位累计 + spacing → 删掉占位（空部分一眼可见）。验证：1280×840 与 900×600 下双轴均 no scroll，内容最小高 304 < 视口 384。教训：上轮我看到 439>376 还宣称"不会滚动"是错的，验证数字必须过线才算数。
- ThemeSwitcher 改为浮动钉左下角（Web fixed 效果）：ThemeSwitcher(parent=窗口) 脱离布局 + raise_() 置顶 + resizeEvent 里重定位（边距16）。验证：parent 是主窗口非布局、(16, h-btn-16) 精确钉住、缩放窗口后仍钉住。
- 题干改 Markdown 渲染（用户提议，采纳）：yaml schema 不动，window 里题干从 Text 换成 Markdown 组件（docs/markdown.md：set_markdown/markdown()/block_renderers；行内 code/粗体/链接富文本，表格走 Table，--- 走 Divider）。验证 17 个 stem 全部 Markdown 渲染。yaml 里现在可以直接写 **粗体**、`code`、``` 代码块、GFM 表格。
- 题号"第 N 题"行高收敛：Subtitle 是 QLabel 走 QLabel 行高（比实际字号大很多），把题号和题干之间撑出大空白。修法：单独成行 + setMaximumHeight(22) 锁死高度。CardBody 上下内边距改为 (10, 12) 上下略偏，留出作答区呼吸。验证 actualH=21。
- 确认组件库 bug 并写文档 E:/projects/python-projects/pyside/HeroSideUI/docs/BUGS.md：BUG-001 Markdown 垂直 Preferred 吸收富余（一行字 sizeHint=24 实际拉到 278，孤立布局最小复现实证）；BUG-002 CardBody(Expanding,Expanding) 致 Card 整体拉高内部 QLabel 大空；BUG-003 Divider 正交轴 Preferred 撑滚动；ISSUE-001 set_text/setText 命名不一致 3 次翻车；ISSUE-002 Subtitle 不收 size；ISSUE-003 Tabs deleteLater 致 _CursorWidget QPainter 崩溃（须用 clear()）。homework 侧均已规避（Markdown 未加 Maximum 前用户端仍见空白——注意：应用侧还没给 stem_md 加 setSizePolicy，修复文档先行）。
- 题干渲染还原为纯 Text（用户实测仍 217px 空白，要求还原，不再坚持 Markdown 方案）：window.py 里 Markdown(q.stem) 换回 Text(q.stem)+setWordWrap，Markdown import 移除。yaml schema 不变（stem 仍是纯字符串，写 Markdown 语法会显示原样字符）。BUGS.md 保留组件库侧记录。教训：offscreen 数字与用户真机渲染不一致时，以用户实测为准；用户两次要求还原时应直接执行。
- 三页流转完成：QStackedWidget 页 0 启动页（两张 is_pressable 大 Card：写作业/批改占位，mousePressEvent 覆盖跳转）→ 页 1 姓名页（Input+returned 信号回车提交、空名红色错误 Text、返回按钮）→ 页 2 作业页（原界面，返回按钮回姓名页）。Input API（docs/input.md）：text()/set_text()/returned 信号。验证：3 页构建、姓名 Jerry 回车→页 2 且 17 渲染器在、逐级返回 2→1→0。注意：clicked.connect(lambda:) 必须包 lambda，直连 setCurrentIndex 返回 None 会 TypeError。
- 启动页调整：去掉 homework-studio 标题；两张模式 Card 居中（垂直：对称 stretch；水平：cards_host 设 (Maximum,Maximum) policy + AlignCenter，Card 定宽 320px）。量坐标要用 mapTo(page)，widget.y() 是相对父级坐标（本轮差点误判）。最终验证 v: 327/326、h: 640/640 双向精确居中。
- 启动页 Card 点击修复：根因是我 mousePressEvent=lambda 覆盖了 is_pressable 的原生事件链（水波纹/光标/pressed 全在原实现里），且子 Text 吃事件。修法：改连官方 pressed 信号 + 内部 Text 设 WA_TransparentForMouseEvents 穿透。模拟点击验证：5/5 卡片角落+中心全部触发跳页。教训：Card 可点就用 is_pressable+pressed，别覆盖事件方法；模拟点击坐标必须 Card 本地坐标（源码 rect().contains 校验本地坐标）。
- 存储方案落地（用户拍板：去分数 + 答案存题库同级）：①全链路删 points/earned/ManualScore → 报告改对错计数（auto_correct/partial/wrong/unanswered + manual_pending/graded/passed）+ ManualReview(passed,comment)；②core/answers.py：student_answers_path=题库同级/<姓名>/<同名>.yaml、sanitize_name 清洗非法字符、save/load/submit（started_at 续做保留、空答案不落盘、坏文件容错）；③UI：渲染器信号→500ms 防抖 QTimer→落盘，closeEvent 兜底，_confirm_name 后挂载作业（首次 pick 修复：初始化无姓名不 pick），submitted 锁提交按钮。编程题 starter 未动视为未作答（collect 返回 None source）。验证：51 测试全绿 + offscreen 全链路（作答→落盘 TestRun/section2.yaml→破坏现场→re-pick 恢复 C/6 全对）。坑：①QTimer 在本执行环境 stdout 管道下 timeout 事件饿死（timer active 但 0 触发），真机事件循环正常——timer 相关验证用直接调 _flush_save() 替代；②Input 无 setText（QLabel 惯用名陷阱再现，restore 静默失败）；③编程题 starter 预填 collect 会带出 source，需在渲染器层过滤。
- 提交前检查完成：测试残留全清（TestRun/unnamed/GuiSmoke），test_app_gui.py 重写适配三页结构（starter 未动=未作答、flush_save+restore、submit 锁定）并在沙箱环境自动 skip（模块级检测 sys.path 含 workbuddy/app.asar）。51 passed 1 skipped。.gitignore 补学生答案目录规则 homework/part*/[!s]*/（人名文件夹不进库、题库 section*.yaml 正常入库）。pyproject 无需改（herosideui==0.8.1 钉死、hatchling 入口 homework=app.main:main 就绪）。待用户 commit。
- 实时保存失效根因破案（用户真机复现"只有关窗才保存"）：CodeEditor text_changed 自触发死循环——PygmentsHighlighter.rehighlight() 改文档标记触发 QPlainTextEdit.textChanged，CodeEditor._on_text_changed 无条件 emit + 重启 150ms 重扫，无人操作也每 150ms 转一圈，500ms 防抖被无限重置。用 traceback spy 抓到 2.5s 内 18 次风暴（code_editor.py:353）。应用侧修复：编程题断开信号改 2s 轮询比对快照（_poll_editors/_editor_snapshot/_poll_timer，flush 后更新快照），其余题型保持信号+防抖。真机验证：单选路径 ~1s 落盘 ✓、编程题轮询路径 ~2.5s 落盘 ✓。组件库 BUGS.md 补 BUG-004（含最小复现+修法建议：rescan 前置布尔抑制对外 emit）。51 passed 1 skipped，探针目录全清（含用户的"测试"文件夹——已确认为作答残留后清理）。
- 批改页完成：GradePage（app/grade_page.py，stack 页 3，启动页批改 Card 激活）。Select 选学生（list_students 扫题库同级人名文件夹）→ 17 题逐卡渲染（题干+学生答案只读+✓/×按钮）；批错展开正确答案区：单选 Select、填空 Input×N、解析 Textarea、编程 CodeEditor（不连 text_changed——BUG-004 风暴，轮询待接）。schema 加 CorrectAnswer(selected/values/text/source)，ManualReview.correct；save_review 写回 reviews 段（保留学生 answers/report/status，report dict 直传 SafeDumper 坑已修）。app/paths.py 抽 BANK_ROOT/PART_LABELS 解循环导入。验收：17 卡全渲染、reviews 落盘（passed+correct）、学生数据完好；53 passed。坑：_student_answer_view code_explain 分支 a=None 炸整卡渲染（4 填空卡连带丢），isinstance 防护统一后过。
- 批改页引发的回归修复：load_answers 签名三元组→四元组（加 reviews）时漏改 window.py:362 的解包，姓名页"下一步"直接 ValueError。全项目 grep 扫描后确认只有该处（grade_page/tests 均已是四元组）。验证：姓名流程通（页 2 + 17 渲染器）、53 passed。教训：改函数返回值形状必须 grep 全部调用点，包括 GUI 入口路径。
- 批改页重写（用户要求完全复刻作业页布局再叠加批改层）：左侧同款 260px 侧栏（Title 批改 + 返回 + 学生 Select + 六部分题库列表含 Divider 分割）；右侧 QStackedWidget（未选学生居中提示 / 作业标题 + 四题型 Tabs + 逐题 Card）。批改层嵌在题卡内：学生答案只读 Text → ✓/×（success/danger flat，已判显示"已判对/已判错"）→ 批错展开正确答案区。验收：1 题库、选学生后 4 Tabs/17 控件/侧栏 23 项、review 落盘。坑：addWidget alignment 魔数 0x84 不行，必须 Qt.AlignmentFlag.AlignCenter。
- 批改页重构（用户口径：批改页=作业页镜像）：GradePage 重写为同构布局（左侧返回+学生 Select，右侧四题型 Tabs），复用 create_renderer 渲染学生作答区→restore 回填→set_disabled(True)（RadioGroup.set_is_disabled / Input·Textarea.set_is_disabled / CodeEditor.set_read_only，四渲染器各加 set_disabled 方法），题卡底部批改控件（✓隐藏correct区 / ×展开correct区：单选Select、填空Input×N、解析Textarea、编程CodeEditor）。_upsert 合并语义：×后编辑correct不冲掉已填内容，✓保留prev.correct。验收：4tabs+17渲染器+17correct组件、回填A/999正确、disabled=True、reviews落盘(passed+selected:C)、学生answers/report/status完好。53 passed。注意：用户 ccc 文件夹是真实作答记录，清理只动自己的探针目录。
- 批改页重构（用户口径：批改页=作业页镜像）：GradePage 重写为同构布局（左侧返回+学生 Select，右侧四题型 Tabs），复用 create_renderer 渲染学生作答区→restore 回填→set_disabled(True)（RadioGroup.set_is_disabled / Input·Textarea.set_is_disabled / CodeEditor.set_read_only，四渲染器各加 set_disabled 方法），题卡底部批改控件（✓隐藏correct区 / ×展开correct区：单选Select、填空Input×N、解析Textarea、编程CodeEditor）。_upsert 合并语义：×后编辑correct不冲掉已填内容，✓保留prev.correct。验收：4tabs+17渲染器+17correct组件、回填A/999正确、disabled=True、reviews落盘(passed+selected:C)、学生answers/report/status完好。53 passed。注意：用户 ccc 文件夹是真实作答记录，清理只动自己的探针目录。
- 批改页高度撑满（用户要求）：Card policy 改 (Expanding, Expanding) + hl.addWidget(card, 1) 均分（去尾部 stretch）+ 作答区 bl.addWidget(renderer.widget(), 1) 吃卡内富余。验证编程 Tab 2 卡各 417px 总 852>780 视口，撑满。与作业页策略不同（作业页贴内容高、批改页撑满）——用户对两页明确不同要求，勿"统一"。
- 批改页两问题修复：①侧栏题库目录（用户重写 GradePage 时被我弄丢）——_load_bank_list 复刻作业页结构（六部分+Divider+作业按钮），点按钮 _on_bank_picked 换题库+刷新学生名单；②Tab 下高度没撑满——根因 Tabs 之前只靠首次 insertWidget 挂载，没稳定 stretch。修：_build_ui 里 Tabs 常驻 _right 且 stretch=1。验证：侧栏 6 部分 1 按钮、编程 Tab 2 卡总高 834 > Tabs 816 填满。
- 学生批改反馈闭环（用户建议 Alert）：_show_review_alerts 在已提交作业每张题卡顶部插 Alert（card.setProperty("body_layout") 登记，insertWidget(1) 题号后）。优先级：reviews 老师批改（通过✓success/未通过✗danger，描述含正确答案+评语）> report 自动判分（correct/partial/wrong/unanswered/pending_manual 六态色）。触发点：_on_assignment_picked（status=submitted 时锁定+显示）和 _on_submit（提交即锁定+显示）。Alert 无 title() getter，读文本用 _title_label.text()/_desc_label.text()。验收：17 Alert 全渲染、批改未通过含示范回答+评语、自动判对/错/部分对全对、作答区锁定。53 passed。
- 学生批改反馈闭环（用户建议 Alert）：_show_review_alerts 在已提交作业每张题卡顶部插 Alert（card.setProperty("body_layout") 登记，insertWidget(1) 题号后）。优先级：reviews 老师批改（通过✓success/未通过✗danger，描述含正确答案+评语）> report 自动判分（correct/partial/wrong/unanswered/pending_manual 六态色）。触发点：_on_assignment_picked（status=submitted 时锁定+显示）和 _on_submit（提交即锁定+显示）。Alert 无 title() getter，读文本用 _title_label.text()/_desc_label.text()。验收：17 Alert 全渲染、批改未通过含示范回答+评语、自动判对/错/部分对全对、作答区锁定。53 passed。
