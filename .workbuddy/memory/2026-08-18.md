- 诊断 PyCharm Jupyter 内核启动失败：项目解释器绑定 D:/programs/python313/python.exe（.iml），该环境只有 pip，无任何 jupyter 包（No module named ipykernel），导致 Kernel process exited before transport became ready。方案：用户自行 pip install jupyter。
- 后续：用户已用 pip 装 ipykernel，但机器级 PIP_TARGET 把包装进 D:/programs/python313/pip-cache-and-globle-packages/packages（不在 sys.path）。方案：写入 D:/programs/python313/Lib/site-packages/custom_pip_target.pth 挂载该目录。验证：import ipykernel OK，jupyter_client 真实拉起内核执行 print(1+1) 成功。
- 用户决定做作业系统：YAML 存题（6 单选+4 填空+3 代码分析+2 编程），PySide + HeroSideUI 做界面。已写 homework/PLAN.md（决策表 D1-D4、YAML schema v1、core 纯函数层、P0-P6 阶段验收、组件映射表、风险清单）。关键设计：代码分析题=single_choice+code 字段；编程题 v1 自评（grading 字段预留 self/manual/tests）。
- PLAN.md 按用户拍板更新：D3=TestPyPI herosideui==0.8.0（必须用 uv 附加索引，pip -i 会整体替换源导致依赖缺失）、D4=手动批改。新增题型：code_explain（自由文本+rubric 手动批改）、programming 作答方式改为传入 .py 文件（ast.parse 只解析不执行，复制归档到 submissions/）。题型共 4 类。
